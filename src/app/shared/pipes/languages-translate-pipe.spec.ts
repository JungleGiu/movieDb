import { LanguagesTranslatePipe } from './languages-translate-pipe';

describe('LanguagesTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new LanguagesTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
