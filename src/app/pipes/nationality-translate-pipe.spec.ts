import { NationalityTranslatePipe } from './nationality-translate-pipe';

describe('NationalityTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new NationalityTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
